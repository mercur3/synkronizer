class Options:
    def __init__(self, home, config, verbose, requests_help):
        self.home = home
        self.config = config
        self.verbose = verbose
        self.requests_help = requests_help
