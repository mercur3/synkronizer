class Options:
    def __init__(self, home, config, verbose=False, requests_help=False):
        self.home = home
        self.config = config
        self.verbose = verbose
        self.requests_help = requests_help
